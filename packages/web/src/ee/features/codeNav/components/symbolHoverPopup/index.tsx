// Placeholder implementation for symbol hover popup
// This is a stub to resolve build issues - full implementation would be part of enterprise edition

import React from 'react';

export interface SymbolHoverPopupProps {
  editorRef?: any;
  language?: string;
  // Add props as needed
  children?: React.ReactNode;
}

export function SymbolHoverPopup(props: SymbolHoverPopupProps) {
  return null; // Stub - no popup in OSS version
}