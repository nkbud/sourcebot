// Placeholder implementation for explore menu
// This is a stub to resolve build issues - full implementation would be part of enterprise edition

import React from 'react';

export interface ExploreMenuProps {
  // Add props as needed
}

export function ExploreMenu(props: ExploreMenuProps) {
  return null; // Stub - no explore menu in OSS version
}